enter candidate

set / interface ethernet-1/1 admin-state enable
set / interface ethernet-1/1 description "to leaf1"
set / interface ethernet-1/1 subinterface 0 admin-state enable
set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
set / interface ethernet-1/1 subinterface 0 ipv4 address 10.1.1.4/31

set / interface ethernet-1/2 admin-state enable
set / interface ethernet-1/2 description "to leaf2"
set / interface ethernet-1/2 subinterface 0 admin-state enable
set / interface ethernet-1/2 subinterface 0 ipv4 admin-state enable
set / interface ethernet-1/2 subinterface 0 ipv4 address 10.1.1.6/31

set / interface system0 admin-state enable
set / interface system0 subinterface 0 admin-state enable
set / interface system0 subinterface 0 ipv4 admin-state enable
set / interface system0 subinterface 0 ipv4 address 10.0.0.2/32

set / system lldp admin-state enable
set / system lldp interface ethernet-1/1 admin-state enable
set / system lldp interface ethernet-1/2 admin-state enable

set / network-instance default type default
set / network-instance default admin-state enable
set / network-instance default interface ethernet-1/1.0
set / network-instance default interface ethernet-1/2.0
set / network-instance default interface system0.0

set / network-instance default next-hop-groups group leaf1-lo nexthop 0 ip-address 10.1.1.5
set / network-instance default next-hop-groups group leaf2-lo nexthop 0 ip-address 10.1.1.7
set / network-instance default static-routes route 10.0.0.3/32 next-hop-group leaf1-lo
set / network-instance default static-routes route 10.0.0.4/32 next-hop-group leaf2-lo

set / network-instance default protocols bgp admin-state enable
set / network-instance default protocols bgp autonomous-system 65000
set / network-instance default protocols bgp router-id 10.0.0.2
set / network-instance default protocols bgp afi-safi ipv4-unicast admin-state enable
set / network-instance default protocols bgp group ibgp peer-as 65000
set / network-instance default protocols bgp group ibgp afi-safi ipv4-unicast admin-state enable
set / network-instance default protocols bgp group ibgp transport local-address 10.0.0.2
set / network-instance default protocols bgp neighbor 10.0.0.3 peer-as 65000
set / network-instance default protocols bgp neighbor 10.0.0.3 peer-group ibgp
set / network-instance default protocols bgp neighbor 10.0.0.4 peer-as 65000
set / network-instance default protocols bgp neighbor 10.0.0.4 peer-group ibgp

commit now
